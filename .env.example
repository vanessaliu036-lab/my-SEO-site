# 複製為 .env.local 後填入值；與 Vercel Project → Settings → Environment Variables（Production）一致，才能「本機 = 線上」對齊。
# 表名固定：OCC_Blog_Posts

AIRTABLE_API_KEY=
# 若後台只發 Personal Access Token，也可用這個變數名（二擇一）
# AIRTABLE_PAT=

AIRTABLE_BASE_ID=
